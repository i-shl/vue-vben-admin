# Vue Vben Admin 项目代码格式规范

## 重要提示
**你必须严格遵守以下所有代码格式规范，生成的代码必须符合项目的 ESLint、Prettier、Stylelint 配置要求。不要生成需要后续格式化的代码！**

---

## 1. Prettier 格式化规范

### 基本规则
- **行宽 (printWidth)**: 80 字符
- **引号**: 使用单引号 `'`，不要使用双引号
- **分号**: 语句末尾必须加分号 `;`
- **尾随逗号**: 对象、数组等末尾必须加逗号 `,`
- **换行符**: 自动检测（LF 或 CRLF）
- **缩进**: 2 个空格
- **插件**: 使用 `prettier-plugin-tailwindcss` 自动排序 Tailwind CSS 类

### 示例
```typescript
// ✅ 正确
const data = {
  name: 'test',
  value: 123,
};

// ❌ 错误
const data = {
  name: "test",
  value: 123
}
```

---

## 2. ESLint 导入排序规范（perfectionist）

### 导入顺序（严格按照以下顺序，组之间用空行分隔）

1. **Type Imports** (type 导入)
   ```typescript
   import type { SomeType } from 'some-package';
   ```

2. **Vue Type Imports** (Vue 相关类型)
   ```typescript
   import type { Component } from 'vue';
   ```

3. **Vben Type Imports** (@vben/* 类型)
   ```typescript
   import type { UserInfo } from '@vben/types';
   ```

4. **Vben Core Type Imports** (@vben-core/* 类型)
   ```typescript
   import type { BaseFormComponentType } from '@vben-core/form-ui';
   ```

5. **External Type Imports** (外部包类型)
   ```typescript
   import type { RouteRecordRaw } from 'vue-router';
   ```

6. **Parent/Sibling/Index Type Imports** (相对路径类型)
   ```typescript
   import type { ComponentType } from './component';
   ```

7. **Internal Type Imports** (#/* 类型)
   ```typescript
   import type { AuthApi } from '#/api/core/auth';
   ```

8. **空行分隔**

9. **Vue Imports** (Vue 相关导入)
   ```typescript
   import { ref, computed, watch } from 'vue';
   ```

10. **Vben Imports** (@vben/* 导入)
    ```typescript
    import { VbenButton, VbenSelect } from '@vben/common-ui';
    ```

11. **Vben Core Imports** (@vben-core/* 导入)
    ```typescript
    import { Card, CardContent } from '@vben-core/shadcn-ui';
    ```

12. **External Imports** (外部包导入)
    ```typescript
    import { Button, message } from 'ant-design-vue';
    ```

13. **Internal Imports** (#/* 导入)
    ```typescript
    import { loginApi } from '#/api/core/auth';
    ```

14. **Parent/Sibling/Index Imports** (相对路径导入)
    ```typescript
    import { useVbenForm } from './form';
    ```

15. **Side-effect Imports** (副作用导入，最后)
    ```typescript
    import './styles.css';
    ```

### 完整导入示例
```typescript
// ✅ 正确
import type { RouteRecordRaw } from 'vue-router';

import { ref, computed } from 'vue';

import { VbenButton } from '@vben/common-ui';
import { Card, CardContent } from '@vben-core/shadcn-ui';
import { Button } from 'ant-design-vue';

import { loginApi } from '#/api/core/auth';
import { useVbenForm } from './form';

import './styles.css';
```

---

## 3. Vue 组件结构规范

### 组件块顺序（严格按照顺序）
```vue
<script setup lang="ts">
// 1. Type imports
// 2. Value imports
// 3. defineOptions
// 4. defineProps
// 5. defineEmits
// 6. 其他代码
</script>

<template>
  <!-- 模板内容 -->
</template>

<style scoped>
/* 样式内容 */
</style>
```

### defineOptions 强制使用
- **所有组件必须使用 `defineOptions`**: 每个 Vue 组件都必须使用 `defineOptions({ name: 'ComponentName' })` 定义组件名
- **组件名使用 PascalCase**: 组件名必须使用 PascalCase 格式
- **位置**: `defineOptions` 必须在导入语句之后、`defineProps` 和 `defineEmits` 之前

### defineOptions, defineProps, defineEmits 顺序
```vue
<script setup lang="ts">
// ✅ 正确顺序
defineOptions({ name: 'ComponentName' });

const props = defineProps<PropsType>();

const emit = defineEmits<EmitsType>();

// ❌ 错误顺序
const props = defineProps<PropsType>();
defineOptions({ name: 'ComponentName' });
```

---

## 4. Vue 组件命名规范

### 组件名
- **PascalCase**: 组件名必须使用 PascalCase
  ```vue
  defineOptions({ name: 'UserProfile' }); // ✅
  defineOptions({ name: 'user-profile' }); // ❌
  ```

### 属性命名
- **kebab-case**: HTML 属性使用 kebab-case
  ```vue
  <VbenButton :show-loading="true" /> <!-- ✅ -->
  <VbenButton :showLoading="true" /> <!-- ❌ -->
  ```

### Props 命名
- **camelCase**: Props 定义使用 camelCase
  ```typescript
  const props = defineProps<{
    showLoading: boolean; // ✅
    'show-loading': boolean; // ❌
  }>();
  ```

### 事件命名
- **kebab-case**: 事件名使用 kebab-case
  ```vue
  @click="handleClick" <!-- ✅ -->
  @click="handle-click" <!-- ❌ -->
  ```

---

## 5. TypeScript 规范

### 类型导入
- **必须使用 `import type`** 导入类型
  ```typescript
  import type { UserInfo } from '@vben/types'; // ✅
  import { UserInfo } from '@vben/types'; // ❌
  ```

### 类型定义
- 使用 `interface` 或 `type` 定义类型
- 类型名使用 PascalCase

---

## 6. HTML/Template 规范

### 属性引号
- **双引号**: HTML 属性值使用双引号
  ```vue
  <div class="container"> <!-- ✅ -->
  <div class='container'> <!-- ❌ -->
  ```

### 自闭合标签
- **组件**: 总是自闭合
  ```vue
  <VbenButton /> <!-- ✅ -->
  <VbenButton></VbenButton> <!-- ❌ -->
  ```

- **普通 HTML**: 非 void 元素不自闭合
  ```vue
  <div></div> <!-- ✅ -->
  <div /> <!-- ❌ -->
  ```

### 多行属性
- 属性过多时，每个属性一行
  ```vue
  <VbenButton
    variant="outline"
    size="sm"
    :loading="isLoading"
    @click="handleClick"
  >
    按钮
  </VbenButton>
  ```

### 模板根节点规范
- **必须使用单个根节点**: 由于项目开启了路由切换动画，模板必须只有一个根节点，否则会导致页面切换后显示空白
- **解决方案**: 在模板最外层添加一个 `<div></div>` 包裹所有内容
- **注意**: template 下的根注释节点也算一个节点，需要避免在根节点同级使用注释

错误示例：
```vue
<template>
  <!-- 注释也算一个节点 -->
  <h1>text h1</h1>
  <h2>text h2</h2>
</template>
```

正确示例：
```vue
<template>
  <div>
    <h1>text h1</h1>
    <h2>text h2</h2>
  </div>
</template>
```

提示：
- 如果想使用多个根标签，可以禁用路由切换动画
- template 下面的根注释节点也算一个节点

---

## 7. CSS/Style 规范

### Tailwind CSS 类排序
- 使用 Prettier 插件自动排序
- 类名之间用空格分隔
  ```vue
  <div class="rounded-xl border border-border bg-card p-6"> <!-- ✅ -->
  <div class="p-6 bg-card border rounded-xl"> <!-- ❌ 未排序 -->
  ```

### CSS 属性顺序（Stylelint）
- 按照以下顺序：
  1. `$variables` (SCSS 变量)
  2. `custom-properties` (CSS 变量)
  3. `at-rules` (@规则)
  4. `declarations` (声明)
  5. `rules` (规则)

---

## 8. 代码风格规范

### 函数定义
- 使用函数声明或箭头函数
- 函数名使用 camelCase
  ```typescript
  function handleClick() {} // ✅
  const handleClick = () => {}; // ✅
  ```

### 变量命名
- **camelCase**: 变量和函数使用 camelCase
- **PascalCase**: 类型和组件使用 PascalCase
- **UPPER_SNAKE_CASE**: 常量使用 UPPER_SNAKE_CASE

### 对象简写
- 优先使用对象简写
  ```typescript
  const name = 'test';
  const obj = { name }; // ✅
  const obj = { name: name }; // ❌
  ```

### 模板字符串
- 优先使用模板字符串
  ```typescript
  const message = `Hello, ${name}`; // ✅
  const message = 'Hello, ' + name; // ❌
  ```

---

## 9. 注释规范

### 单行注释
- 使用 `//`，注释前加空格
  ```typescript
  // 这是注释
  const value = 123;
  ```

### 多行注释
- 使用 `/* */`
  ```typescript
  /*
   * 这是多行注释
   * 第二行
   */
  ```

### 禁止 console.log
- **不允许使用 `console.log`**
- 允许使用 `console.warn` 和 `console.error`
  ```typescript
  console.warn('警告信息'); // ✅
  console.error('错误信息'); // ✅
  console.log('调试信息'); // ❌
  ```

---

## 10. 文件结构规范

### Vue 单文件组件
- 文件扩展名: `.vue`
- 文件名: PascalCase 或 kebab-case
  ```
  UserProfile.vue ✅
  user-profile.vue ✅
  ```

### TypeScript 文件
- 文件扩展名: `.ts` 或 `.tsx`
- 文件名: kebab-case
  ```
  user-service.ts ✅
  userService.ts ❌
  ```

---

## 11. 特殊规则

### 空行规则
- 导入组之间必须有空行
- 函数之间建议有空行
- 逻辑块之间建议有空行

### 行尾空格
- **禁止行尾空格**
- 文件末尾必须有换行符

### 最大行长度
- 单行不超过 80 字符
- 超过时换行

---

## 12. Vue 特殊规则

### HTML 属性换行
- 属性过多时，每个属性一行，闭合标签单独一行
  ```vue
  <VbenButton
    variant="outline"
    size="sm"
    :loading="isLoading"
    @click="handleClick"
  >
    按钮文本
  </VbenButton>
  ```

### 多行 HTML 元素内容
- 多行内容时，开始和结束标签各占一行
  ```vue
  <div>
    内容1
    内容2
  </div>
  ```

### 单行 HTML 元素内容
- 单行内容时，可以写在一行
  ```vue
  <div>单行内容</div>
  ```

### v-bind 简写
- 优先使用 `:` 简写，不要使用 `v-bind:`
  ```vue
  <Component :prop="value" /> <!-- ✅ -->
  <Component v-bind:prop="value" /> <!-- ❌ -->
  ```

### v-on 简写
- 优先使用 `@` 简写，不要使用 `v-on:`
  ```vue
  <Component @click="handleClick" /> <!-- ✅ -->
  <Component v-on:click="handleClick" /> <!-- ❌ -->
  ```

---

## 13. 代码生成检查清单

生成代码前，请确认：

- [ ] 所有导入已按正确顺序排列，组之间有空行
- [ ] 使用单引号，不是双引号
- [ ] 语句末尾有分号
- [ ] 对象/数组末尾有尾随逗号
- [ ] TypeScript 类型使用 `import type` 导入
- [ ] Vue 组件块顺序正确（script -> template -> style）
- [ ] 所有组件都使用了 defineOptions 定义组件名
- [ ] defineOptions 在 defineProps 之前
- [ ] HTML 属性使用双引号
- [ ] 组件名使用 PascalCase
- [ ] Props 使用 camelCase
- [ ] 事件名使用 kebab-case
- [ ] Tailwind CSS 类已排序（使用 Prettier 插件）
- [ ] 没有 console.log（只允许 console.warn 和 console.error）
- [ ] 代码行长度不超过 80 字符
- [ ] 缩进为 2 个空格
- [ ] 没有行尾空格
- [ ] v-bind 使用 `:` 简写
- [ ] v-on 使用 `@` 简写
- [ ] 多行属性每个属性一行
- [ ] template 只有一个根节点（避免页面切换空白问题）

---

## 14. 示例：完整的 Vue 组件

```vue
<script setup lang="ts">
import type { RouteRecordRaw } from 'vue-router';

import { ref, computed } from 'vue';

import { VbenButton, VbenSelect } from '@vben/common-ui';
import { Button } from 'ant-design-vue';

import { loginApi } from '#/api/core/auth';

defineOptions({ name: 'UserLogin' });

const isLoading = ref(false);
const formData = ref({
  username: '',
  password: '',
});

const canSubmit = computed(() => {
  return formData.value.username && formData.value.password;
});

async function handleSubmit() {
  isLoading.value = true;
  try {
    await loginApi(formData.value);
  } catch (error) {
    console.error('登录失败:', error);
  } finally {
    isLoading.value = false;
  }
}
</script>

<template>
  <div class="space-y-4 p-6">
    <VbenButton
      :disabled="!canSubmit"
      :loading="isLoading"
      @click="handleSubmit"
    >
      登录
    </VbenButton>
  </div>
</template>

<style scoped></style>
```

---

## 15. 严格遵循

**重要**: 生成的代码必须完全符合以上所有规范。如果代码不符合规范，用户将需要手动修复格式化问题，这会增加额外的工作量。

**请确保生成的代码可以直接通过 ESLint、Prettier 和 Stylelint 检查，无需任何后续格式化！**

